{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "$id": "http://data.europa.eu/eudi/TrustMarkResource.json",
  "title": "Trust Mark Resource",
  "description": "Defines the TrustMarkResource object, including a single image and its associated localised texts.",
  "type": "object",
  "properties": {
    "image": {
      "title": "Image Resource",
      "type": "object",
      "description": "The image resource properties.",
      "properties": {
        "name": {
          "description": "The filename or identifier for the image.",
          "type": "string"
        },
        "url": {
          "description": "The relative path or full URL to the image.",
          "type": "string",
          "format": "uri-reference"
        }
      },
      "required": ["name", "url"],
      "additionalProperties": false
    },
    "text": {
      "title": "Text Resource",
      "type": "object",
      "description": "The text resource properties and localisations.",
      "properties": {
        "name": {
          "description": "Trust Mark user information, used for rendering in the EUDI Wallet user interface.",
          "type": "string"
        },
        "localisations": {
          "description": "An object mapping language codes (e.g., 'en', 'fr') to localised text strings.",
          "type": "object",
          "additionalProperties": {
            "type": "string"
          },
          "minProperties": 1
        }
      },
      "required": ["name", "localisations"],
      "additionalProperties": false
    }
  },
  "required": ["image", "text"],
  "additionalProperties": false
}