schemaVersion: 1
requirements:
  - id: VEND-INT-001
    deploymentArchitectures:
      - intermediary
    category: contracts
    requirement: Display both intermediary and RP identity before requesting attestations
    explanation: |
      When the intermediary requests attestations from a wallet user, the wallet
      must display both the intermediary's identity (as the direct requester) and
      the identity of the ultimate Relying Party on whose behalf the intermediary
      is acting. This ensures informed consent.

      **Wallet detection mechanism (RPI_07):**
      The wallet detects an intermediary scenario by comparing two data sources:
      - The intermediary's identity (name, identifier) from its **access
        certificate** (obtained via RPI_01 registration)
      - The intermediated RP's identity (name, identifier) from the **RPRC_19a
        extension** in the presentation request, and from the RP's registration
        certificate if available (obtained via RPI_03)

      If these identities differ, the wallet knows the request is from an
      intermediary and displays both names. The intermediary product must
      therefore ensure both data sources are correctly populated in every
      presentation request.

      **Data that must be populated (RPI_05 → RPRC_19a):**
      The intermediated RP must supply to the intermediary: (a) its
      user-friendly name, (b) its unique identifier, (c) the URL of its
      Registrar, (d) the identifier of its intended use, and (e) a
      user-friendly description of its intended use. Since (a)-(c) are
      static, they can be configured once; (d)-(e) vary only if the RP
      has multiple registered intended uses.

      **Relationship verification (RPI_07a):**
      If the user requests it, the wallet will additionally verify that
      the contractual relationship between intermediary and RP is
      registered at the responsible Registrar (per RPI_04). Failure to
      register this relationship will cause the wallet to warn the user,
      potentially blocking the transaction.
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "8"
    arfReference:
      topic: Topic 52
      hlr:
        - RPI_06
        - RPI_07
        - RPRC_19a
    legalText: |
      When an intermediary requests attestations from a wallet unit, the wallet 
      unit shall display both the identity of the intermediary and the identity 
      of the relying party on whose behalf the intermediary is acting.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-002
    deploymentArchitectures:
      - intermediary
    category: contracts
    requirement: Ensure RP's intended data requests are accurately communicated to wallet
    explanation: |
      The intermediary must not modify, expand, or alter the RP's registered
      data request when forwarding to wallets. Only the attributes registered
      by the RP with its Registrar may be requested.

      **Request scope guarantee (RPI_06 + Art 5b(3)):**
      The intermediary SHALL include in the request only those attributes that
      the intermediated RP has registered for the selected intended use. The
      wallet can verify this: per RPRC_21, the wallet may query the Registrar
      using the intended-use identifier from RPRC_19a to retrieve the list of
      attributes the RP is authorised to request. Any discrepancy between the
      RPRC_19a request and the registered attribute set is a compliance
      violation.

      **Prohibited intermediary behaviours:**
      - Adding attributes beyond the RP's registration
        ("attribute expansion")
      - Removing attributes the RP expects ("attribute suppression")
      - Modifying attribute type identifiers or namespaces
      - Requesting attributes for the intermediary's own purposes under
        the RP's identity

      This is a MUST NOT obligation because over-requesting undermines user
      consent and may trigger DPA complaints via the wallet's reporting
      mechanism.
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "3"
    legalText: |
      Relying parties shall not request users to provide any data other than
      that indicated pursuant to paragraph 2, point (c).
    arfReference:
      topic: Topic 52
      hlr: RPI_06
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST NOT
  - id: VEND-INT-003
    deploymentArchitectures:
      - intermediary
    category: usage
    requirement: Forward presentation requests without modification of requested attributes
    explanation: |
      The intermediary must accurately relay the RP's attribute request to the
      wallet without adding, removing, or modifying the requested attributes.
      The request must match what the RP registered with supervisory
      authorities.

      **Differentiation from VEND-INT-002:**
      While VEND-INT-002 addresses the **scope** constraint (only registered
      attributes may be requested), this requirement addresses the
      **forwarding integrity** guarantee: the technical relay mechanism must
      preserve the request exactly as constructed.

      **Technical relay requirements (RPI_06):**
      When the intermediated RP triggers a presentation request, the
      intermediary SHALL construct the OID4VP or ISO 18013-5 request
      containing:
      - The intermediary's own access certificate (RPI_01)
      - The RP's registration certificate, if available (RPI_03)
      - The RPRC_19a extension fields for the intermediated RP (RPI_05)
      - The exact set of requested attributes as specified by the RP

      The product must ensure no transformation, filtering, or enrichment
      occurs between the RP's internal request and the wallet-facing
      protocol message. This is especially important for selective
      disclosure: the attribute identifiers and namespaces must be
      bit-exact with the RP's registration.
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "3"
    legalText: |
      Relying parties shall not request users to provide any data other than
      that indicated pursuant to paragraph 2, point (c).
    arfReference:
      topic: Topic 52
      hlr: RPI_06
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-004
    deploymentArchitectures:
      - intermediary
    category: usage
    requirement: Forward wallet presentations to RP without modification
    explanation: |
      The intermediary must relay the wallet's presentation to the RP exactly
      as received, without altering, extracting, or processing the credential
      data. The presentation integrity must be preserved.

      **Forwarding after verification (RPI_08 + RPI_09):**
      After receiving a presentation from a Wallet Unit, the intermediary
      SHALL first perform the verifications specified in RPI_09 (authenticity,
      revocation status, device binding, user binding, combined presentation
      validity). Only if all verifications succeed SHALL the intermediary
      forward the attributes to the requesting RP. If any verification
      fails, the intermediary SHALL NOT forward any attributes.

      **No-storage mandate (Article 5b(10)):**
      The intermediary is deemed a Relying Party and SHALL NOT store data
      about the content of the transaction. This means:
      - No persistent storage of attestation payloads
      - No caching of attribute values between sessions
      - No logging of individual attribute content
      - Memory-only processing with secure erasure after forwarding

      **Integrity preservation:**
      The presentation payload must be forwarded without modification so
      that the RP can independently verify signatures and selective-
      disclosure proofs if it chooses to do so.
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "10"
    legalText: |
      Intermediaries acting on behalf of relying parties shall be deemed to be
      relying parties and shall not store data about the content of the transaction.
    arfReference:
      topic: Topic 52
      hlr:
        - RPI_08
        - RPI_09
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST NOT
  - id: VEND-INT-005
    deploymentArchitectures: []
    category: usage
    requirement: Support both remote and proximity presentation protocols
    explanation: |
      If the RP requires both remote and proximity (offline) presentation
      scenarios, the connector must support the relevant protocols for
      each mode.

      **Remote presentations:**
      - OpenID for Verifiable Presentations (OID4VP) per ISO/IEC TS
        18013-7:2024
      - SD-JWT VC credential format with HAIP profile
      - Both same-device (app-to-app) and cross-device (QR code) flows
      - `direct_post` and `direct_post.jwt` response modes

      **Proximity presentations:**
      - ISO/IEC 18013-5:2021 device retrieval protocol
      - BLE as the mandatory transport, NFC as optional
      - Device engagement via QR code or NFC tap
      - mDoc credential format

      **Architecture-specific considerations:**
      For proximity flows, the connector's deployment must include
      hardware (BLE/NFC readers) at the RP's physical locations. The
      intermediary must handle the additional complexity of performing
      RPI_09 verification on-site before forwarding to the RP backend.
      For remote flows, the connector must correctly route the
      `redirect_uri` / `response_uri` callbacks through its own
      infrastructure while preserving the RP's session context.

      The obligation is conditional: if the RP only uses remote flows,
      only OID4VP is required. If the RP only uses proximity flows,
      only ISO 18013-5 is required. If both modes are used, both
      MUST be supported.
    legalBasis:
      regulation: 2024/2982
      article: Article 5
      paragraph: "1"
    legalText: |
      Wallet providers shall ensure that wallet solutions support protocols
      and interfaces for the presentation of person identification data and
      electronic attestations of attributes.
    annexReference: |
      Annex to 2024/2982:
      - ISO/IEC 18013-5:2021 (proximity)
      - ISO/IEC TS 18013-7:2024 (remote)
    arfReference:
      topic: Topic 24
      hlr: ProxId_01a
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-006
    deploymentArchitectures:
      - intermediary
    category: usage
    requirement: Forward attributes only to the requesting RP after verification
    explanation: |
      After receiving attestation data from a wallet, the intermediary must
      perform all agreed verifications (RPI_09) and forward the attributes
      ONLY to the specific RP on whose behalf the request was made. If any
      verification fails, no attributes may be forwarded.

      **Verification gate (RPI_09):**
      Before forwarding, the intermediary SHALL verify (as agreed with the
      RP):
      - Authenticity of the PID or attestation (signature validation)
      - Revocation status
      - Device binding (WSCD key proof-of-possession)
      - User binding
      - Combined presentation validity (if multiple attestations)
      - Wallet Unit authenticity and revocation status

      **Conditional forwarding (RPI_08):**
      If ALL verifications succeed → forward attributes only to the
      requesting RP. If ANY verification fails → forward NOTHING. The
      intermediary must never partially forward verified attributes.

      **Routing isolation:**
      When serving multiple RPs, the intermediary must enforce strict
      routing isolation to prevent attributes from being delivered to
      the wrong RP. This requires per-session RP binding from request
      initiation through to attribute delivery.

      **Immediate deletion (RPI_10):**
      After forwarding (or after verification failure), the intermediary
      SHALL delete all PIDs, attestations, and user attributes completely
      and immediately (see also VEND-INT-021).
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "10"
    legalText: |
      Intermediaries acting on behalf of relying parties shall be deemed to be
      relying parties and shall not store data about the content of the transaction.
    arfReference:
      topic: Topic 52
      hlr:
        - RPI_08
        - RPI_09
        - RPI_10
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-007
    deploymentArchitectures: []
    category: usage
    requirement: Maintain up-to-date Trusted Lists for PID Providers
    explanation: |
      The connector must maintain current copies of the Trusted Lists
      containing PID Provider trust anchors (public keys/certificates).
      These lists are published by Member States and the Commission and
      must be refreshed regularly to capture additions, modifications,
      and revocations.

      **Signature validation requirement (OIA_12):**
      For both proximity and remote presentation flows, the intermediary
      SHALL validate the signature of a PID using a trust anchor provided
      in a PID Provider Trusted List made available in accordance with
      Topic 31 (notification and publication). This is a prerequisite for
      the RPI_09 verification gate that determines whether attributes
      may be forwarded to the RP.

      **Trust list discovery and refresh:**
      Member States publish Trusted Lists in a standardised format. The
      EU Commission publishes a List of Trusted Lists (LOTL) that
      aggregates the Member State lists. The connector should:
      - Bootstrap from the LOTL to discover all Member State lists
      - Refresh lists at intervals aligned with the lists' own
        `nextUpdate` timestamps (typically 24-72 hours)
      - Handle list signature verification (the list itself is signed)
      - Maintain a fallback if a Member State list is temporarily
        unavailable

      **Architecture-specific note:**
      Because connectors serve multiple RPs potentially operating
      across different Member States, they may need to maintain Trusted
      Lists from all 27 Member States simultaneously, rather than only
      the subset relevant to a single RP's market.
    legalBasis:
      regulation: 2014/910
      article: Article 22
      paragraph: "1"
    legalText: |
      Each Member State shall establish, maintain and publish trusted lists,
      including information related to the qualified trust service providers
      for which it is responsible.
    arfReference:
      topic: Topic 1
      hlr: OIA_12
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-008
    deploymentArchitectures: []
    category: usage
    requirement: Maintain up-to-date Trusted Lists for Attestation Providers
    explanation: |
      The connector must maintain current copies of Trusted Lists for
      qualified and non-qualified electronic attestation of attributes
      (QEAA/EAA) providers. These trust anchors are essential for
      validating attestation signatures as part of the RPI_09
      verification gate.

      **QEAA Provider validation (OIA_13):**
      For both proximity and remote flows, the connector SHALL validate
      the qualified signature of a QEAA in accordance with Art 32 of the
      eIDAS Regulation. The trust anchor must come from a QEAA Provider
      Trusted List made available in accordance with Art 22.

      **PuB-EAA Provider validation (OIA_14):**
      For PuB-EAAs, the connector SHALL validate the qualified
      signature using the public key from the qualified certificate of
      the QTSP, and SHALL also validate the QTSP's certificate itself
      using a trust anchor from the appropriate Trusted List.

      **Non-qualified EAA validation (OIA_15):**
      For non-qualified EAAs, trust anchors are provided according to
      mechanisms specified in the applicable Rulebook (Topic 12). The
      connector may also need to verify that the EAA Provider is
      authorised to issue the specific type of attestation.

      **Trust anchor isolation:**
      The connector must distinguish between trust anchors for PIDs
      (OIA_12, see VEND-INT-007), QEAAs (OIA_13), PuB-EAAs (OIA_14),
      and non-qualified EAAs (OIA_15). A trust anchor valid for one
      category must not inadvertently validate attestations from another.
    legalBasis:
      regulation: 2014/910
      article: Article 22
      paragraph: "1"
    legalText: |
      Each Member State shall establish, maintain and publish trusted lists,
      including information related to the qualified trust service providers
      for which it is responsible.
    arfReference:
      topic: Topic 1
      hlr:
        - OIA_13
        - OIA_14
        - OIA_15
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-009
    deploymentArchitectures: []
    category: usage
    requirement: Obtain trust anchors only from official Trusted List sources
    explanation: |
      Trust anchors for verification must be obtained exclusively from
      official EU and Member State Trusted Lists, not from third-party
      aggregators or unofficial sources. This ensures the authenticity
      of verification keys and prevents supply-chain attacks on the
      trust infrastructure.

      **Official sources (OIA_14):**
      The eIDAS Regulation mandates that trust anchors come from Trusted
      Lists published by Member States (Art 22) or the Commission. The
      intermediary must use these authoritative sources directly, or
      verify the chain of custody if a distribution point
      is used.

      **Prohibited practices:**
      - Using third-party trust anchor aggregation services without
        verifying their integrity against official sources
      - Manually importing trust anchors from unverified downloads
      - Accepting trust anchors from RP customers or other parties
      - Using hardcoded trust anchors that bypass Trusted List updates

      **Verification of Trusted List authenticity:**
      Each Trusted List is itself signed. The connector must verify
      the Trusted List's signature using the Member State's certificate
      before extracting trust anchors. This creates a chain of trust:
      Commission LOTL → Member State TL → Provider trust anchor →
      Attestation signature.
    arfReference:
      topic: Topic 1
      hlr: OIA_14
    legalBasis:
      regulation: 2014/910
      article: Article 22
      paragraph: "1"
    legalText: |
      Each Member State shall establish, maintain and publish trusted lists,
      including information related to the qualified trust service providers
      for which it is responsible.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-010
    deploymentArchitectures: []
    category: usage
    requirement: Validate attestation signatures using official trust anchors
    explanation: |
      The connector must verify the cryptographic signatures on
      attestations and PIDs using the public keys from official Trusted
      Lists. Invalid or untrusted signatures must result in rejection.

      **Signature validation as RPI_09 prerequisite:**
      Attestation authenticity verification is the first check in the
      RPI_09 verification gate. Without a valid signature, no attributes
      may be forwarded to the RP (per RPI_08). The connector must
      verify:
      - The attestation's issuer signature against the trust anchor
      - The certificate chain from the issuer to the Trusted List root
      - That the trust anchor is from the correct category (PID vs
        QEAA vs PuB-EAA vs non-qualified EAA)

      **Cryptographic algorithm support:**
      The connector must support the algorithms mandated by the
      ARF and implementing acts, including ECDSA with P-256/P-384
      curves for SD-JWT VC and COSE signatures for mDoc format.
      Algorithm agility is important as new algorithms may be
      approved over time.

      **Rejection handling:**
      When a signature is invalid, the connector must:
      - Reject the entire presentation (not just the invalid attestation)
      - Log the verification failure with sufficient detail for dispute
        resolution (without logging attestation content)
      - Return an appropriate error to the RP
      - Immediately delete all received attestation data (per RPI_10)
    legalBasis:
      regulation: 2024/2979
      article: Article 8
      paragraph: "1"
    legalText: |
      The validity of electronic attestations of attributes shall be
      verified using the trust anchors published in official Trusted Lists.
    arfReference:
      topic: Topic 1
      hlr:
        - OIA_12
        - OIA_13
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-011
    deploymentArchitectures: []
    category: usage
    requirement: Verify attestation revocation status using supported mechanisms before acceptance (recommended)
    explanation: |
      Before accepting a presentation as valid, the connector should
      check the revocation status of the attestation. This is a
      recommended (SHOULD) step in the RPI_09 verification gate.

      **VCR_13 — Recommended verification:**
      Per VCR_13, a Relying Party Instance SHOULD verify the
      revocation status of a PID or attestation upon obtaining it
      from a Wallet Unit. This is a SHOULD (recommended), not a
      SHALL (mandatory), meaning the connector may choose not
      to verify revocation in low-risk scenarios. However, if the
      connector and RP agree that revocation checking is required
      (per RPI_09 agreement scope), then it becomes effectively
      mandatory for that relationship.

      **VCR_12 — Dual-mechanism support:**
      If the connector decides to verify revocation status, it SHALL
      support BOTH mechanisms specified per VCR_11:
      - **Attestation Status Lists**: compressed bitstring lists
        where each bit represents the status of one attestation;
        efficient for batch checking without per-credential queries
      - **Attestation Revocation Lists (Identifier Lists)**: lists
        of revoked attestation identifiers; useful for targeted
        checks

      **Short-lived attestation exception (VCR_01):**
      If an attestation has a validity period of 24 hours or less
      (short-lived), revocation checking is unnecessary because the
      attestation will expire before a revocation could take effect.

      **Verification failure handling (RPI_08):**
      If revocation verification fails (attestation is found to be
      revoked), the connector SHALL NOT forward attributes to the
      RP per RPI_08. Status lists must be fetched from PID/EAA
      Providers via the URLs specified in the attestation metadata.

      **Operational considerations:**
      - Status lists should be cached to minimise latency, with refresh
        intervals aligned to the issuer's publication frequency.
        Cache strategy must balance freshness (detecting recent
        revocations) against performance (avoiding per-request fetches).
      - When revocation endpoints are unavailable, the connector must
        decide: reject the presentation (fail-closed) or accept with a
        warning (fail-open). This policy should be configurable per RP.
      - Revocation checking must not log individual attestation
        identifiers, as this could create a tracking vector (no-storage
        mandate applies).
    legalBasis:
      regulation: 2024/2979
      article: Article 3
      paragraph: "1"
    legalText: |
      Wallet providers shall ensure that wallet solutions verify the
      validity of electronic attestations of attributes.
    arfReference:
      topic: Topic 7
      hlr:
        - VCR_11
        - VCR_12
        - VCR_13
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    notes: |
      This covers ARF Topic 7 (VCR_*) verification requirements
      for attestation revocation. Attestation Status Lists (VCR_11)
      are the primary mechanism for efficient batch revocation
      checking without per-credential queries.
      Consolidated from VEND-INT-011 + VEND-INT-034.
    obligation: SHOULD
  - id: VEND-INT-012
    deploymentArchitectures: []
    category: usage
    requirement: Verify the wallet unit's device binding
    explanation: |
      The connector must verify that the presentation is bound to the
      wallet unit's secure cryptographic device (WSCD), confirming that
      the presenter controls the private key associated with the
      attestations. This is a core check in the RPI_09 verification gate.

      **Device binding verification (RPI_09):**
      The connector SHALL verify device binding by checking the
      proof-of-possession mechanism:
      - For mDoc (ISO 18013-5): verify the `deviceAuth` structure,
        which contains either a MAC or ECDSA signature over session
        data using the device's private key
      - For SD-JWT VC: verify the Key Binding JWT (`kb-jwt`), which
        proves possession of the key referenced in the `cnf` claim

      **What device binding proves:**
      - The presentation was created by the wallet that holds the
        attestation (not copied or exported)
      - The wallet's WSCD generated the proof in real-time (combined
        with freshness checks in VEND-INT-013)
      - The attestation has not been cloned or replayed from another
        device

      **Failure handling:**
      If device binding verification fails, the connector SHALL NOT
      forward any attributes (per RPI_08). This is a hard failure — the
      entire presentation is rejected, and the RP is notified of the
      verification failure.
    legalBasis:
      regulation: 2024/2979
      article: Article 3
      paragraph: "1"
    legalText: |
      Wallet providers shall ensure that wallet solutions support
      protocols and interfaces that allow verification of the
      authenticity of the wallet unit.
    arfReference:
      topic: Topic 52
      hlr: RPI_09
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-013
    deploymentArchitectures: []
    category: security
    requirement: Verify presentation freshness and prevent replay attacks
    explanation: |
      The connector must verify that presentations are fresh and not
      replayed. This includes checking nonces, timestamps, or session
      binding, and implementing mechanisms to detect replay of
      previously received presentations. Stale or replayed
      presentations must be rejected.

      **Freshness mechanisms:**
      - **Nonce challenge-response**: the connector generates a
        cryptographically random nonce included in the presentation
        request; the wallet embeds this nonce in the response,
        signed by the device key
      - **Timestamp validation**: the presentation contains a creation
        timestamp, which the connector validates against a
        configurable time window (accounting for clock skew)
      - **Session binding**: the OID4VP `state` parameter binds the
        wallet response to the specific browser/client session

      **Replay detection (Article 5b(9)):**
      The connector must implement server-side nonce tracking to
      detect replay attempts. Once a nonce has been used, it must be
      marked as consumed and rejected if re-submitted. The nonce store
      must be:
      - Persistent across intermediary restarts (within the nonce's
        validity window)
      - Distributed if the connector runs in a multi-instance
        deployment
      - Automatically pruned after nonce expiry

      **Combined with device binding (RPI_09):**
      Freshness verification and device binding together ensure that
      the presentation was created by the legitimate wallet user in
      real-time, not replayed from a captured session.
    legalBasis:
      - regulation: 2024/2979
        article: Article 3
        paragraph: "1"
      - regulation: 2014/910
        article: Article 5b
        paragraph: "9"
    legalText: |
      Relying parties shall implement appropriate measures to prevent
      fraud and ensure the authenticity of person identification data
      and electronic attestations of attributes.
    arfReference:
      topic: Topic 52
      hlr: RPI_09
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-014
    deploymentArchitectures:
      - intermediary
    category: security
    requirement: Ensure secure channel for transmission to RP
    explanation: |
      The intermediary must establish and maintain a secure, encrypted
      channel when forwarding wallet presentations to the RP. This
      protects the confidentiality and integrity of attestation data
      in transit between the intermediary and the RP backend.

      **Transport requirements (RPI_08):**
      The channel used to forward attributes from the intermediary to
      the RP must provide:
      - **Confidentiality**: TLS 1.3 (or TLS 1.2 with approved cipher
        suites as a minimum). TLS 1.0 and 1.1 must be disabled.
      - **Server authentication**: the RP's endpoint must be
        authenticated via its TLS certificate
      - **Mutual authentication (recommended)**: mTLS provides
        bidirectional authentication, ensuring the intermediary can
        prove its identity to the RP and vice versa
      - **Perfect forward secrecy**: cipher suites using ephemeral
        key exchange (ECDHE) to protect past sessions if long-term
        keys are compromised

      **Application-layer protection:**
      In addition to transport encryption, the intermediary should
      consider application-layer protection such as:
      - JWE (JSON Web Encryption) for payload encryption
      - Signed verification results (JWS) so the RP can verify the
        intermediary's verification outcome is authentic
      - API authentication (OAuth 2.0 client credentials, API keys)

      **Failure handling:**
      If the secure channel cannot be established (e.g., TLS handshake
      failure, certificate validation error), the intermediary SHALL
      NOT forward any attributes. The presentation data must be deleted
      immediately per RPI_10.
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "10"
    legalText: |
      Intermediaries acting on behalf of relying parties shall be deemed
      to be relying parties and shall not store data about the content
      of the transaction.
    arfReference:
      topic: Topic 52
      hlr: RPI_08
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-016
    deploymentArchitectures: []
    category: security
    requirement: Implement secure key management for verification operations
    explanation: |
      The connector must implement secure storage and handling of any
      cryptographic material used in verification operations. Keys must
      be protected against unauthorised access, modification, and
      extraction.

      **Key types requiring protection:**
      - **TLS private keys** for the connector's server certificate
        (inbound from wallets) and client certificate (outbound to RPs
        if mTLS is used)
      - **Ephemeral session keys** generated during OID4VP or ISO
        18013-5 device engagement
      - **Signing keys** for verification result JWS signatures
        (if the connector signs its verification outcomes)
      - **Trust anchor certificates** (public keys, but integrity must
        be protected against tampering)

      **Storage requirements:**
      - Private keys SHOULD be stored in HSMs (Hardware Security
        Modules) or equivalent secure enclaves for production
        deployments
      - At minimum, keys must be encrypted at rest using AES-256 or
        equivalent, with key encryption keys stored separately
      - Keys must never appear in logs, error messages, or diagnostic
        output

      **Lifecycle management:**
      - Key generation with sufficient entropy (CSPRNG)
      - Automated rotation schedules with overlap periods
      - Secure destruction when decommissioned (cryptographic erasure)
      - Emergency revocation procedures for compromised keys
    legalBasis:
      regulation: 2024/2979
      article: Article 3
      paragraph: "1"
    legalText: |
      Wallet providers shall ensure that wallet solutions implement
      appropriate technical and organisational measures to ensure a
      level of security appropriate to the risk.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-017
    deploymentArchitectures:
      - intermediary
    category: security
    requirement: Protect verification results integrity
    explanation: |
      The verification result communicated to the RP must be protected
      against tampering. The RP must be able to trust that the
      intermediary's verification outcome is authentic and unmodified.

      **Integrity protection mechanisms (RPI_08):**
      The intermediary should protect verification results using:
      - **JWS (JSON Web Signature)** for signing the verification
        outcome, creating a non-repudiable proof of the intermediary's
        verification decision
      - **HMAC** with a shared secret for simpler deployments where
        non-repudiation is not required
      - **TLS channel binding** as a baseline transport-level
        protection (necessary but not sufficient alone)

      **Verification result payload:**
      The signed result should include:
      - Verification outcome (pass/fail per RPI_09 check)
      - Timestamp of verification
      - Nonce binding (linking the result to the original request)
      - List of checks performed and their individual outcomes
      - Intermediary's identifier (for accountability)

      **Non-repudiation:**
      If the intermediary signs its verification results with a JWS,
      the RP can independently verify the result's authenticity and
      retain the signed result as evidence for dispute resolution,
      without needing to retain the underlying attestation data.
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "10"
    legalText: |
      Intermediaries acting on behalf of relying parties shall be deemed
      to be relying parties and shall not store data about the content
      of the transaction.
    arfReference:
      topic: Topic 52
      hlr: RPI_08
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-018
    deploymentArchitectures: []
    category: security
    requirement: Maintain audit logs of verification operations
    explanation: |
      The connector should maintain logs of verification operations
      including verification timestamps, outcomes, and any errors.
      These logs support dispute resolution and compliance auditing
      while respecting privacy (no attestation content should be
      logged).

      **What may be logged (content-free metadata):**
      - Verification timestamp (ISO 8601)
      - Session/request identifier (internal correlation ID)
      - RP identifier (which RP the request was for)
      - Verification outcome per RPI_09 check (pass/fail)
      - Error codes and failure reasons
      - Protocol used (OID4VP / ISO 18013-5)
      - Processing duration

      **What must NOT be logged (no-storage mandate):**
      - Individual attribute names or values from the attestation
      - PID content (name, date of birth, etc.)
      - Wallet unit identifiers that could enable user tracking
      - Presentation payloads or fragments thereof
      - Device binding keys or key material

      **Log integrity and retention:**
      - Logs should be tamper-evident (append-only, signed, or
        blockchain-anchored) to support dispute resolution
      - Retention period should be defined per RP agreement and
        aligned with GDPR data minimisation
      - Logs must themselves be protected as processing records
        under GDPR Article 30
    legalBasis:
      regulation: 2016/679
      article: Article 32
      paragraph: "1"
    legalText: |
      The controller and processor shall implement measures to ensure
      a process for regularly testing, assessing and evaluating the
      effectiveness of technical and organisational measures for
      ensuring the security of processing.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    notes: |
      Logs must not contain attestation content (due to no-storage mandate)
      but may contain verification metadata (pass/fail, timestamp, error codes).
    obligation: MUST
  - id: VEND-INT-019
    deploymentArchitectures:
      - intermediary
      - direct_saas
    category: usage
    requirement: Do not extract or log individual attribute values from presentations
    explanation: |
      Beyond the no-storage mandate, intermediaries must not extract,
      log, or analyse individual attribute values from wallet
      presentations. The presentation should be treated as an opaque
      payload for forwarding.

      **Sealed pipeline architecture:**
      The intermediary's processing pipeline should be designed so that
      attestation content is never decoded into individual attributes.
      Verification operations (RPI_09 checks) require cryptographic
      validation of signatures, device binding, and revocation status,
      but these operations can be performed without parsing the
      attribute-level content.

      **Prohibited actions:**
      - Parsing SD-JWT VC disclosures to extract individual claims
      - Decoding mDoc `IssuerSignedItem` elements to read attribute
        values
      - Logging attribute names or values in any form
      - Indexing or searching over presentation content
      - Performing analytics, profiling, or ML training on content

      **Tension with verification:**
      Some RPI_09 checks (e.g., user binding, where the attestation
      may contain a user photo or biometric template) may require
      limited attribute access. If such access is necessary, it must
      be minimal, purpose-limited, and the accessed data must be
      securely erased immediately after the verification step.
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "10"
    legalText: |
      Intermediaries acting on behalf of relying parties shall be deemed
      to be relying parties and shall not store data about the content
      of the transaction.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST NOT
  - id: VEND-INT-020
    deploymentArchitectures:
      - intermediary
      - direct_saas
    category: usage
    requirement: Do not correlate or link user presentations across different RPs
    explanation: |
      Intermediaries serving multiple RPs must not correlate wallet
      user activity across different RP interactions. Each RP
      relationship must be treated independently to preserve user
      unlinkability.

      **Unlinkability as a fundamental right (Article 5a(16a)):**
      The eIDAS Regulation explicitly mandates that providers of
      electronic identity means shall not collect information about
      the use of the wallet that is not necessary for the provision
      of the service. This extends to intermediaries who, by serving
      multiple RPs, have a unique vantage point for cross-RP tracking.

      **Architectural isolation requirements:**
      - **Session isolation**: each RP's sessions must be logically
        separated; session identifiers must not enable cross-RP linking
      - **Data isolation**: no shared databases, caches, or data
        structures where user-identifiable data from different RP
        contexts could be joined
      - **Log isolation**: verification logs must not contain common
        identifiers (wallet unit IDs, device keys) that could be
        correlated across RP boundaries
      - **Staff access controls**: intermediary personnel must not
        have the ability to query across RP tenant boundaries

      **Technical anti-correlation mechanisms:**
      - Avoid storing or processing wallet unit identifiers beyond
        the immediate session
      - Use per-RP, per-session correlation IDs that are meaningless
        outside their context
      - Ensure nonce stores are partitioned per RP to prevent
        cross-RP session linking
    legalBasis:
      regulation: 2014/910
      article: Article 5a
      paragraph: "16"
    legalText: |
      Providers of electronic identification means shall not collect
      information on the use of the European Digital Identity Wallet
      that is not necessary for the provision of the wallet services.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST NOT
  - id: VEND-INT-021
    deploymentArchitectures:
      - intermediary
    category: usage
    requirement: Delete attestation data immediately after forwarding to RP
    explanation: |
      The intermediary must completely delete all PIDs, attestations,
      and user attributes immediately after sending them to the
      intermediated RP. If forwarding fails or is not performed
      (e.g., due to verification failure), deletion must still occur
      immediately after completing all necessary verifications.

      **RPI_10 requirements:**
      The intermediary SHALL delete any PIDs or attestations it
      obtained from the Wallet Unit, including any user attributes,
      completely and immediately after sending them to the RP. If
      the intermediary does not send any attributes (e.g., because
      a verification failed), deletion must occur immediately after
      completing all verifications.

      **Scope of deletion:**
      - Attestation payloads in memory (secure zeroing, not just
        deallocation)
      - Any temporary files created during processing
      - In-memory caches or buffers
      - Database records (if any were created during processing)
      - Log entries that may contain attestation fragments
      - Swap/page file entries (consider memory locking to prevent
        paging)

      **Verification of deletion:**
      The product should provide auditable evidence of deletion:
      - Deletion timestamp recorded in the verification log
      - Memory zeroing confirmation (e.g., via secure memory APIs)
      - File system confirmation (if temporary files were used)

      **Failure scenarios:**
      If the RP's endpoint is unreachable and forwarding fails, the
      intermediary MUST still delete immediately. Retry mechanisms
      that would require retaining attestation data are prohibited
      by the no-storage mandate.
    arfReference:
      topic: Topic 52
      hlr: RPI_10
    legalBasis:
      regulation: 2014/910
      article: Article 5b
      paragraph: "10"
    legalText: |
      Intermediaries acting on behalf of relying parties shall be deemed
      to be relying parties and shall not store data about the content
      of the transaction.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-022
    deploymentArchitectures:
      - intermediary
    category: contracts
    requirement: Specify RP details in each presentation request to wallet
    explanation: |
      When requesting attestations from a wallet, the intermediary must
      include complete information about the intermediated RP via the
      RPRC_19a extension in the presentation request.

      **Required RPI_05 fields:**
      The intermediated RP must supply the following to the
      intermediary, which SHALL include them in each request:
      - (a) User-friendly name of the intermediated RP
      - (b) Unique identifier of the intermediated RP
      - (c) URL of the Registrar where the RP is registered
      - (d) Identifier of the intended use (selecting the applicable
        registered use)
      - (e) User-friendly description of the intended use

      Since (a)-(c) are static, they can be configured once during
      RP onboarding. Items (d)-(e) are also static if the RP has
      only one registered intended use, but must be dynamically
      selected per request if the RP has multiple uses.

      **Registration certificate inclusion:**
      If the intermediated RP has registration certificates (per
      RPI_03), it SHALL also indicate which single registration
      certificate the intermediary must include in the request.

      **Wallet behaviour enabled by this data:**
      This data enables the wallet to:
      - Display accurate RP information to the user (per RPI_07)
      - Verify that requested attributes match what the RP registered
        (per RPRC_21)
      - Detect that the request is from an intermediary rather than a
        direct RP (by comparing RPRC_19a identity with the access
        certificate identity)
    legalBasis:
      regulation: 2025/848
      article: Annex I
      paragraph: "9"
    legalText: |
      For each intended use, a list of the data, including attestations
      and attributes, that the relying party intends to request, a
      user-friendly name and a technical name, the attestation type
      and a description of intended use of the data.
    arfReference:
      topic: Topic 52
      hlr:
        - RPI_05
        - RPRC_19a
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-023
    deploymentArchitectures: []
    category: usage
    requirement: Support mandatory credential formats based on supported use cases
    explanation: |
      Relying Party connectors and connectors must support the
      credential formats relevant to their operational use cases.

      **Remote Presentations (online services):**
      - **SD-JWT-VC** format (Selective Disclosure JSON Web Token for
        Verifiable Credentials) as the primary credential format
      - **OpenID4VP** protocol for presentation exchange (same-device
        and cross-device flows via `direct_post` response mode)
      - The connector must verify SD-JWT VC signatures, validate
        selective disclosures, and check key binding JWTs

      **Proximity Presentations (in-person verification):**
      - **ISO/IEC 18013-5:2021 (mDoc)** format for proximity
        credential presentation
      - **BLE and/or NFC** transport protocols per ISO/IEC 18013-5
        and ISO/IEC 18013-7
      - The connector must handle device engagement, session
        establishment, CBOR decoding, and COSE signature verification

      **Use-case dependency (RPRC_20a):**
      Per RPRC_20a, Relying Party Instances and Wallet Units SHALL
      support the extension for ISO/IEC 18013-5 OR the extension for
      OpenID4VP (as applicable). The "or" indicates use-case
      dependency:
      - Online-only RPs: SD-JWT-VC / OpenID4VP required
      - In-person-only RPs: mDoc / ISO 18013-5 required
      - Mixed-mode RPs: BOTH formats required

      **Attestation Rulebook impact (ARB_02):**
      The Attestation Rulebook for each attestation type determines
      whether proximity presentation is required. If so, the Rulebook
      mandates mDoc format issuance, meaning the connector must
      support mDoc verification for those attestation types.

      Note: This differs from Issuers, who must unconditionally
      support both formats (per ISSU_12).
    legalBasis:
      regulation: 2024/2979
      article: Article 8
      paragraph: "1"
    legalText: |
      Wallet solutions shall support the technical standards and
      specifications set out in Annex II for the formats of PIDs and
      electronic attestations of attributes.
    annexReference: |
      Annex II of 2024/2979:
      - ISO/IEC.18013-5:2021
      - 'Verifiable Credentials Data Model 1.1', W3C Recommendation
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    arfReference:
      topic: Topic 3
      hlr:
        - PID_02
        - RPRC_20a
        - ARB_02
    notes: |
      Per ARF RPRC_20a: "Relying Party Instances and Wallet Units SHALL support
      the extension for [ISO/IEC 18013-5] or the extension for [OpenID4VP]..."

      The "or" indicates use-case dependency:
      - Online-only RPs: SD-JWT-VC / OpenID4VP required
      - In-person-only RPs (e.g., age verification at venue): mDoc / ISO 18013-5 required
      - Mixed-mode RPs: BOTH formats required

      This contrasts with VEND-ISS-005 where issuers must unconditionally
      support both formats per ISSU_12.
    obligation: MUST
  - id: VEND-INT-024
    deploymentArchitectures: []
    category: usage
    requirement: Support selective disclosure in both credential formats
    explanation: |
      The connector's verification implementation must correctly
      handle selectively disclosed attributes in both mDoc and VC
      formats. Verification must succeed even when only a subset of
      attributes is presented.

      **SD-JWT-VC selective disclosure:**
      - The SD-JWT VC format uses hash-based selective disclosure:
        the issuer includes salted hashes of each claim, and the
        holder reveals only the claims they choose by including the
        corresponding disclosure values
      - The connector must verify the hash of each disclosed claim
        against the issuer-signed SD-JWT payload
      - Undisclosed claims must be ignored (not treated as missing
        or invalid)
      - The key binding JWT (`kb-jwt`) must be verified to confirm
        holder authorisation

      **mDoc selective disclosure:**
      - The mDoc format uses `IssuerSignedItem` elements within
        namespaces; the holder selects which elements to release
      - The connector must verify the Mobile Security Object
        (MSO) signature and check that each disclosed element's
        digest matches the MSO
      - Undisclosed elements must be treated as absent, not as
        verification failures

      **Architecture-specific considerations:**
      The connector must forward exactly the disclosed subset to
      the RP (per VEND-INT-004). It must not infer, reconstruct,
      or flag undisclosed attributes. The verification pipeline
      should be format-agnostic at the forwarding stage.
    legalBasis:
      regulation: 2024/2982
      article: Article 5
      paragraph: "4"
    legalText: |
      Wallet solutions shall enable the wallet user to select which
      attributes within an attestation to present to a relying party.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    arfReference:
      topic: Topic 12
      hlr: ARB_02
    obligation: MUST
  - id: VEND-INT-025
    deploymentArchitectures: []
    category: registration
    requirement: Consider certification for cryptographic components
    explanation: |
      Depending on the assurance level required by the RP, the
      connector may need to demonstrate that its cryptographic
      implementations meet recognised standards. This is especially
      relevant for high-assurance use cases.

      **Applicable certification frameworks:**
      - **FIPS 140-2/3** (Level 1–4): US NIST standard for
        cryptographic modules; widely accepted in EU for cross-border
        interoperability
      - **Common Criteria (ISO/IEC 15408)**: EAL4+ is typical for
        security-critical components; relevant under the EU
        Cybersecurity Act (Regulation 2019/881)
      - **SOG-IS Crypto Evaluation Scheme**: European scheme for
        cryptographic evaluation (relevant for Member State
        requirements)
      - **eIDAS Article 24 audits**: QTSPs must be audited by a
        conformity assessment body; connectors supporting QTSPs
        may inherit audit requirements

      **Scope of certification:**
      Certification may cover:
      - Cryptographic libraries used for signature verification
        (ECDSA P-256/P-384, EdDSA, RSA)
      - HSM or secure enclave hardware
      - Random number generators (DRBG per SP 800-90A)
      - TLS implementation (cipher suite compliance)
      - Key storage and lifecycle management

      **Conditional nature:**
      This is a risk-based requirement. Not all connectors will
      require full certification, but the connector should be
      prepared to provide certification evidence when requested by
      high-assurance RPs or national supervisory authorities.
    legalBasis:
      regulation: 2019/881
      article: Article 47
      paragraph: "1"
    legalText: |
      A European cybersecurity certification scheme shall be designed
      to achieve, as applicable, the following security objectives.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    notes: |
      This is a conditional requirement based on the RP's risk assessment and
      the sensitivity of the use case. Not all connectors will require
      certification.
    obligation: MUST
  - id: VEND-INT-026
    deploymentArchitectures: []
    category: usage
    requirement: Comply with embedded disclosure policies in attestations
    explanation: |
      Electronic attestations of attributes may contain embedded
      disclosure policies that specify conditions the relying party
      must meet to access the attestation. The connector must
      verify RP compliance with these policies and ensure the wallet
      can inform the user of the result.

      **Disclosure policy mechanics:**
      An embedded disclosure policy is a machine-readable set of
      conditions attached to individual attributes or to the
      attestation as a whole. These conditions may include:
      - **RP category requirements**: only certain RP types (e.g.,
        financial institutions, healthcare providers) may access
        specific attributes
      - **Purpose limitations**: attributes may only be used for
        specified purposes (e.g., age verification, identity
        proofing)
      - **Jurisdiction restrictions**: attributes may only be
        disclosed to RPs in certain jurisdictions
      - **Temporal constraints**: attributes may only be disclosed
        during certain time windows

      **Connector responsibilities:**
      - Parse the disclosure policy from the attestation metadata
      - Evaluate the intermediated RP's registration against the
        policy conditions (using Registrar data)
      - Communicate the evaluation result to the wallet (pass/fail)
        so the wallet can inform the user
      - If the RP does not meet the policy, the connector must
        NOT forward the request to the wallet (or the wallet will
        reject it)

      **Interaction with RPI_06:**
      The disclosure policy check is complementary to the RPI_06
      attribute scope check: RPI_06 ensures the RP only requests
      attributes it has registered for, while the disclosure policy
      ensures the RP meets the conditions set by the attestation
      issuer.
    legalBasis:
      regulation: 2024/2979
      article: Article 10
      paragraph: "3"
    legalText: |
      Wallet instances shall verify whether the wallet-relying party
      complies with the requirements of the embedded disclosure
      policy and inform the wallet user of the result.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-027
    deploymentArchitectures: []
    category: usage
    requirement: Support WebAuthn specification for pseudonym generation
    explanation: |
      When the connector supports pseudonymous authentication from
      wallet units, it must support the WebAuthn specification as
      mandated in Annex V of the Integrity Regulation. This enables
      privacy-preserving authentication without revealing user
      identity.

      **How pseudonyms work in the EUDI Wallet context:**
      - The wallet generates a WebAuthn credential (public/private
        key pair) scoped to a specific RP
      - The public key serves as the pseudonym — a stable identifier
        that the RP can use for return visits without knowing the
        user's real identity
      - The private key remains in the wallet's WSCD and is used to
        sign authentication challenges

      **Connector verification requirements:**
      - Verify the WebAuthn assertion signature using the registered
        public key (pseudonym)
      - Support at minimum the `ES256` algorithm (ECDSA with P-256)
        per WebAuthn recommendations
      - Handle `authenticatorData` fields including RP ID hash,
        flags, and sign count
      - Forward the verified pseudonym to the intermediated RP
        without associating it with any PID or attestation data

      **Privacy implications:**
      The intermediary sees the pseudonym during verification but
      must not store or correlate it across sessions (per the
      no-storage mandate). The pseudonym should be treated with the
      same deletion requirements as attestation data (per RPI_10).
    legalBasis:
      regulation: 2024/2979
      article: Article 14
      paragraph: "1"
    legalText: |
      Wallet units shall support the generation of pseudonyms for
      wallet users in compliance with the technical specifications
      set out in Annex V.
    annexReference: |
      Annex V of 2024/2979:
      - 'Web Authentication', W3C Recommendation
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-028
    deploymentArchitectures:
      - intermediary
    category: usage
    requirement: Support relying-party-specific pseudonyms
    explanation: |
      Wallet units generate unique pseudonyms per relying party,
      ensuring unlinkability across different RPs. The intermediary
      must correctly handle these RP-specific pseudonyms in
      verification flows.

      **RP-specific pseudonym mechanics:**
      - The wallet derives a unique key pair per RP using the RP's
        identifier (typically the RP ID in WebAuthn terms, derived
        from the RP's domain or registered identifier)
      - Different RPs receive different pseudonyms for the same
        user, making cross-RP correlation impossible at the
        cryptographic level

      **Intermediary-specific challenges:**
      Because the intermediary operates on behalf of multiple RPs,
      it must ensure:
      - The RP ID used for pseudonym derivation is the
        **intermediated RP's** identifier, not the intermediary's
        own identifier — otherwise all RPs behind the intermediary
        would receive the same pseudonym, defeating unlinkability
      - The intermediary does not maintain any mapping between
        pseudonyms and real identities (or between pseudonyms of
        the same user across different RPs)
      - Pseudonym handling follows the same no-storage mandate as
        attestation data: the pseudonym is forwarded to the RP and
        then deleted

      **Interaction with VEND-INT-020:**
      This requirement reinforces the cross-RP unlinkability mandate.
      Even if the intermediary processes the pseudonyms of the same
      user for two different RPs, those pseudonyms are
      cryptographically distinct and should be treated as unrelated.
    legalBasis:
      regulation: 2024/2979
      article: Article 14
      paragraph: "2"
    legalText: |
      A pseudonym shall be relying-party specific, meaning that only
      wallets and relying parties can link different transactions to
      the same pseudonym.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-029
    deploymentArchitectures: []
    category: usage
    requirement: Use valid Wallet-Relying Party Access Certificate
    explanation: |
      The connector must use a valid Wallet-Relying Party Access
      Certificate (issued by its Registrar) when interacting with
      wallet units. The certificate identifies the intermediary and
      enables the wallet to verify its registration status.

      **RPA_01 authentication mechanism:**
      The access certificate is part of the RP authentication
      mechanism that SHALL:
      - Enable the Wallet Unit to identify and authenticate the
        Relying Party (i.e., the intermediary)
      - Enable the Wallet Unit to verify that the request was not
        copied and replayed
      - Use certificates issued in accordance with Topic 27
        (Registration of Relying Parties)

      **Architecture-specific certificate usage:**
      The connector's access certificate identifies the
      **intermediary itself**, not the intermediated RP. The
      intermediated RP's identity is conveyed via the RPRC_19a
      extension (per VEND-INT-022). The wallet detects the
      intermediary scenario by comparing these two identities
      (per RPI_07).

      **Certificate lifecycle requirements:**
      - Certificates have finite validity; the connector must
        monitor expiration and renew before expiry
      - If a certificate is revoked (e.g., because the intermediary
        loses its registration), all wallet interactions will fail
      - The connector should support automated renewal and have
        fallback procedures for unexpected revocation

      **Protocol-specific certificate presentation:**
      - **OID4VP (remote)**: the access certificate is presented
        during TLS handshake (server certificate) and/or in the
        request JWT
      - **ISO 18013-5 (proximity)**: the access certificate is
        included in the reader authentication structure
    legalBasis:
      regulation: 2024/2982
      article: Article 3
      paragraph: "1"
    legalText: |
      Wallet providers shall ensure that wallet solutions support
      protocols and interfaces which allow wallet-relying parties
      to present a valid wallet-relying party access certificate.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    arfReference:
      topic: Topic 6
      hlr:
        - RPA_01
        - RPI_07
    obligation: MUST
  - id: VEND-INT-030
    deploymentArchitectures:
      - intermediary
      - direct_saas
    category: usage
    requirement: Implement TS7 data deletion request interface for GDPR Article 17 compliance
    explanation: |
      The intermediary must implement the common data deletion
      request interface specified in Technical Specification 7
      (TS7). When a user requests erasure of their data from an RP
      (via the wallet), the intermediary must process or relay the
      request.

      **DATA_DLT_07 — User authentication:**
      Before executing a data deletion request, the intermediary
      SHALL authenticate the requesting user using appropriate
      authentication mechanisms. The intermediary SHOULD use the
      authentication or signature facilities offered by the user's
      Wallet Unit for this purpose. This means the deletion request
      flow may involve a presentation exchange specifically for
      authentication (not for attribute disclosure).

      **DATA_DLT_08 — TS7 compliance:**
      Wallet Units, Relying Parties, and Registrars SHALL comply
      with the relevant requirements in TS7. For the intermediary,
      this means implementing the TS7 API endpoint(s) that wallet
      units will use to submit deletion requests.

      **Intermediary-specific relay pattern:**
      Because the intermediary operates on behalf of an
      intermediated RP, deletion requests may need to be relayed:
      - The wallet sends the deletion request to the intermediary
        (since that is the entity the wallet interacted with)
      - The intermediary authenticates the user (per DATA_DLT_07)
      - The intermediary relays the request to the intermediated
        RP (the actual data controller)
      - The intermediary confirms processing to the wallet

      **No-storage interaction (VEND-INT-021):**
      The intermediary's no-storage mandate means minimal personal
      data will be retained. However, any operational metadata
      (audit logs, transaction records, error logs) that could
      contain personal data must still be subject to erasure
      requests. The intermediary should be able to identify and
      purge such data when a deletion request is received.

      **GDPR Article 17 timeframes:**
      The controller (intermediary or RP) must erase personal data
      "without undue delay" — typically interpreted as within one
      month, with possible extension for complex requests.
    legalBasis:
      regulation: 2016/679
      article: Article 17
      paragraph: "1"
    legalText: |
      The data subject shall have the right to obtain from the
      controller the erasure of personal data concerning him or
      her without undue delay and the controller shall have the
      obligation to erase personal data without undue delay.
    arfReference:
      topic: Topic 48
      hlr:
        - DATA_DLT_07
        - DATA_DLT_08
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-031
    deploymentArchitectures:
      - intermediary
      - direct_saas
    category: contracts
    requirement: Support user reporting to data protection authorities
    explanation: |
      Wallet units allow users to report RPs to data protection
      authorities (DPAs) if they suspect unlawful data requests.
      The intermediary's identity (as the entity making the request
      on behalf of the RP) will be visible to users and may be
      included in such reports.

      **How DPA reporting works in the EUDI Wallet ecosystem:**
      - The wallet provides a reporting mechanism per Article 51
        of the GDPR (supervisory authorities)
      - The user can report an RP that they believe requested
        excessive or unlawful data
      - The report will include the RP's identity as displayed
        during the consent flow

      **Intermediary visibility in reports:**
      In an intermediated scenario, the wallet displays both the
      intermediary's identity (from its access certificate) and
      the intermediated RP's identity (from RPRC_19a). A user
      report may include either or both identities:
      - The intermediary (as the entity that technically requested
        the data)
      - The intermediated RP (as the entity that instructed the
        intermediary to request the data)

      **Intermediary preparedness:**
      The intermediary should be prepared to:
      - Respond to DPA inquiries about its intermediation activities
      - Provide evidence of which RP instructed each request
      - Demonstrate that it complied with the no-storage mandate
      - Show that it only requested attributes registered for the
        RP's intended use (per RPI_06)
      - Maintain sufficient audit trails to support investigations
        without retaining personal data
    legalBasis:
      regulation: 2024/2982
      article: Article 7
      paragraph: "1"
    legalText: |
      Wallet providers shall ensure that wallet units allow wallet
      users to easily report wallet-relying parties to supervisory
      authorities established under Article 51 of Regulation (EU)
      2016/679.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    obligation: MUST
  - id: VEND-INT-032
    deploymentArchitectures:
      - intermediary
    category: usage
    requirement: Include intermediary access certificate and RP registration certificate in requests
    explanation: |
      When requesting attestations from a wallet on behalf of an
      intermediated RP, the intermediary must include both:
      (1) its own access certificate (proving its identity as a
      registered intermediary), and (2) the registration certificate
      of the intermediated RP, if the RP has one. This enables the
      wallet to verify both the intermediary and the ultimate
      service provider.

      **Dual-certificate structure:**
      - **Intermediary access certificate**: issued by the
        intermediary's Registrar pursuant to RPA_01 and Topic 27;
        identifies the intermediary as a registered entity
      - **RP registration certificate**: obtained during the
        intermediary's registration of the intermediated RP (per
        RPI_03); contains the RP's registered attributes, intended
        uses, and identity

      **Protocol-specific inclusion:**
      - **OID4VP (remote)**: the intermediary's access certificate
        is part of the TLS handshake or request JWT; the RP's
        registration certificate is included via the RPRC_19a
        extension fields
      - **ISO 18013-5 (proximity)**: both certificates are included
        in the reader authentication structure

      **Wallet verification flow:**
      The wallet uses the dual-certificate to:
      - Authenticate the intermediary (using its access certificate)
      - Verify the intermediated RP's registration (using the RP's
        registration certificate, if available)
      - Check the intermediary-RP relationship (per RPI_07a)
      - Display both identities to the user (per RPI_07)

      **When the RP has no registration certificate:**
      If the intermediated RP does not have a registration
      certificate (e.g., because it has not registered intended
      uses), the intermediary includes only its own access
      certificate. The wallet may warn the user that the RP's
      registration cannot be fully verified.
    arfReference:
      topic: Topic 52
      hlr:
        - RPI_06
        - RPI_03
        - RPI_07
    legalBasis:
      regulation: 2025/848
      article: Article 8
      paragraph: "2"
    legalText: |
      When requested by an intermediated Relying Party, an
      intermediary SHALL request a presentation of attributes from
      a specific Wallet Unit. In the request, the intermediary
      SHALL include the intermediary's access certificate and the
      registration certificate of the Relying Party, if available.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    notes: |
      This is the technical implementation of RPI_06. The access
      certificate comes from the intermediary's own registration.
      The RP registration certificate is obtained during the
      intermediary's registration of each intermediated RP (per
      RPI_03).
    obligation: MUST
  - id: VEND-INT-033
    deploymentArchitectures:
      - intermediary
    category: contracts
    requirement: Ensure intermediary-RP contractual relationship is registered
    explanation: |
      Before the intermediary can act on behalf of an RP, the
      contractual relationship between them must be registered with
      the relevant Registrar. The wallet verifies this registration
      when processing requests (per RPI_07a).

      **RPI_04 registration requirement:**
      When registering an intermediated Relying Party, the
      intermediary SHALL provide legally valid evidence that the
      RP will indeed use the intermediary's services to interact
      with Wallet Units. Such evidence may be:
      - A signed contract between the intermediary and the RP
      - A service agreement with explicit wallet interaction scope
      - A formal authorisation letter from the RP
      The Registrar SHALL verify this evidence and, if correct,
      register the relationship.

      **RPI_07a wallet verification:**
      When a Wallet Unit receives a request from an intermediary
      on behalf of an intermediated RP, and the user wants to
      verify the RP's registration, the wallet SHALL verify that
      the intermediary-RP relationship is registered. This can be
      done by:
      - Inspecting the registration certificate (if available)
      - Querying the Registrar directly
      If verification fails, the wallet SHALL notify the user when
      asking for consent — the user may still proceed, but with a
      warning.

      **Onboarding lifecycle:**
      The intermediary must ensure registration is complete BEFORE
      accepting intermediation requests from an RP. The typical
      lifecycle is:
      1. Contract signing between intermediary and RP
      2. Intermediary submits registration request to Registrar
         with contractual evidence
      3. Registrar verifies evidence and registers the relationship
      4. Intermediary obtains the RP's registration certificate
         (per RPI_03)
      5. Intermediary begins accepting wallet interaction requests
         for that RP

      **Relationship termination:**
      When the intermediary-RP relationship ends, the intermediary
      must notify the Registrar to de-register the relationship.
      Failure to do so means wallets will still see the
      relationship as active, potentially misleading users.
    arfReference:
      topic: Topic 52
      hlr:
        - RPI_04
        - RPI_07a
    legalBasis:
      regulation: 2025/848
      article: Article 6
      paragraph: "5"
    legalText: |
      When registering an intermediated Relying Party, an
      intermediary SHALL provide legally valid evidence that this
      Relying Party will indeed use the services of this
      intermediary to interact with Wallet Units. The Registrar
      SHALL verify this evidence and register the relationship
      between the intermediary and the intermediated Relying Party.
    roles:
      - relying_party
    productCategories:
      - connector
    deadline: "2027-11-21"
    notes: |
      This corresponds to ARF RPI_04 and supports wallet
      verification per RPI_07a. The intermediary must ensure
      registration is complete BEFORE accepting intermediation
      requests from an RP, otherwise wallet users will see
      verification failures.
    obligation: MUST
